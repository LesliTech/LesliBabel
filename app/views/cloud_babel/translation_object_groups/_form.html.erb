<%= form_with(model: translation_object_group, local: true) do |form| %>
  <% if translation_object_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(translation_object_group.errors.count, "error") %> prohibited this translation_object_group from being saved:</h2>

      <ul>
        <% translation_object_group.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
