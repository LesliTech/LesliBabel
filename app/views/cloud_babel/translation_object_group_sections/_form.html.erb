<%= form_with(model: translation_object_group_section, local: true) do |form| %>
  <% if translation_object_group_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(translation_object_group_section.errors.count, "error") %> prohibited this translation_object_group_section from being saved:</h2>

      <ul>
        <% translation_object_group_section.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
