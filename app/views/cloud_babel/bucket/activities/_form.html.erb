<%= form_with(model: bucket_activity, local: true) do |form| %>
  <% if bucket_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bucket_activity.errors.count, "error") %> prohibited this bucket_activity from being saved:</h2>

      <ul>
        <% bucket_activity.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
