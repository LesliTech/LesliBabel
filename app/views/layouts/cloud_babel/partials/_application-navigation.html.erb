<%#

Copyright (c) 2020, all rights reserved.

All the information provided by this platform is protected by international laws related  to 
industrial property, intellectual property, copyright and relative international laws. 
All intellectual or industrial property rights of the code, texts, trade mark, design, 
pictures and any other information belongs to the owner of this platform.

Without the written permission of the owner, any replication, modification,
transmission, publication is strictly forbidden.

For more information read the license file including with this software.

// · ~·~     ~·~     ~·~     ~·~     ~·~     ~·~     ~·~     ~·~     ~·~     ~·~     ~·~     ~·~
// · 

%>

<template>

    <%= nav_link(root_path) do %>
        <i class="fas fa-tachometer-alt"></i>
        <span>Dashboard</span>
    <% end %>

    <%= nav_link(translations_path) do %>
        <i class="fas fa-language"></i>
        <span>Translations</span>
    <% end %>
    
    <%= nav_link(modules_path) do %>
        <i class="fas fa-laptop"></i>
        <span>Modules &amp; devices</span>
    <% end %>

    <% if not current_page?(root_url) %>
        <p class="menu-label">Languages</p>
        <% Rails.application.config.lesli_settings["configuration"]["locales_available"].each do |language| %>
            <%= navigation_item("?language=en") do %>
                <i class="flag-icon flag-icon-<%= language[0] == "en" ? "gb": language[0] %>"></i>
                <span><%= language[1] %></span>
            <% end %>
        <% end %>
    <% end %>

    <p class="menu-label">Modules</p>

    <%# CloudBabel::Module.where.not(:platform => ["rails_engine", "rails_core"]).each do |engine| %>
    <% CloudBabel::Module.all.order(:name).each do |engine| %>
        <%= navigation_item(modules_path + "/" + engine.id.to_s) do %>
            <i class="far fa-folder-open"></i>
            <span><%= engine.name.gsub("Cloud", "") %></span>
        <% end %>
    <% end %>

</template>
