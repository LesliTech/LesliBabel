<%= form_with(model: translation_module, local: true) do |form| %>
  <% if translation_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(translation_module.errors.count, "error") %> prohibited this translation_module from being saved:</h2>

      <ul>
        <% translation_module.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
