<%= form_with(model: module_discussion, local: true) do |form| %>
  <% if module_discussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(module_discussion.errors.count, "error") %> prohibited this module_discussion from being saved:</h2>

      <ul>
        <% module_discussion.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
