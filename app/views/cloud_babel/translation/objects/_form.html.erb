<%= form_with(model: translation_object, local: true) do |form| %>
  <% if translation_object.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(translation_object.errors.count, "error") %> prohibited this translation_object from being saved:</h2>

      <ul>
        <% translation_object.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
