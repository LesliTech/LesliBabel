<%= form_with(model: translation_string, local: true) do |form| %>
  <% if translation_string.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(translation_string.errors.count, "error") %> prohibited this translation_string from being saved:</h2>

      <ul>
        <% translation_string.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
